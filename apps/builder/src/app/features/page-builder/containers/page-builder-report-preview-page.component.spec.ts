import { PageBuilderReportPreviewPageComponent } from './page-builder-report-preview-page.component';

describe('PageBuilderReportPreviewPageComponent', () => {
  it('should be defined', () => {
    expect(PageBuilderReportPreviewPageComponent).toBeDefined();
  });
});
