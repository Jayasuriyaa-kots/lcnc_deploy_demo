import { ChartLiveWidgetComponent } from './chart-live-widget.component';

describe('ChartLiveWidgetComponent', () => {
  it('should be defined', () => {
    expect(ChartLiveWidgetComponent).toBeDefined();
  });
});
