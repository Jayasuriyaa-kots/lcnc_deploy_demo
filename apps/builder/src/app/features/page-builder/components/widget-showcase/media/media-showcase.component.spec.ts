import { MediaShowcaseComponent } from './media-showcase.component';

describe('MediaShowcaseComponent', () => {
  it('should be defined', () => {
    expect(MediaShowcaseComponent).toBeDefined();
  });
});
