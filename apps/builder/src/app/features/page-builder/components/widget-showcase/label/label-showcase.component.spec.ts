import { LabelShowcaseComponent } from './label-showcase.component';

describe('LabelShowcaseComponent', () => {
  it('should be defined', () => {
    expect(LabelShowcaseComponent).toBeDefined();
  });
});
