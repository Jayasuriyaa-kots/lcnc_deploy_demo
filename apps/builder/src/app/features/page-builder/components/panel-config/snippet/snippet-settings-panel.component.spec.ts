import { SnippetSettingsPanelComponent } from './snippet-settings-panel.component';

describe('SnippetSettingsPanelComponent', () => {
  it('should be defined', () => {
    expect(SnippetSettingsPanelComponent).toBeDefined();
  });
});
