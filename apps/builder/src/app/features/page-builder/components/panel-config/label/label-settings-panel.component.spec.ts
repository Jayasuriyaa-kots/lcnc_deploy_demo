import { LabelSettingsPanelComponent } from './label-settings-panel.component';

describe('LabelSettingsPanelComponent', () => {
  it('should be defined', () => {
    expect(LabelSettingsPanelComponent).toBeDefined();
  });
});
