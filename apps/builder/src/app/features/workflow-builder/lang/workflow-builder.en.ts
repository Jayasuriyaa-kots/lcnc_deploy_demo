import {
  WORKFLOW_LANGUAGE,
  type WorkflowLanguage,
} from '../services/workflow-language';

export { WORKFLOW_LANGUAGE, type WorkflowLanguage };

/** Alias aligned with scope name `workflow-builder`. */
export const WORKFLOW_BUILDER_LANG = WORKFLOW_LANGUAGE;
